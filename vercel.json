{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd landing && npm install",
  "buildCommand": "cd landing && npm run build",
  "outputDirectory": "landing/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
