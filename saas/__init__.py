"""VibeSafe SaaS layer — web API, auth, billing, and usage limits.

This package wraps the core CLI scanner (via orchestrator) with a
multi-user web service. It is entirely optional; the CLI works without it.
"""
